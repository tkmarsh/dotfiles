[alias]
    unstage = reset HEAD --
    last = log -1 HEAD
    lg = log --all --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'\n--abbrev-commit --date=relative
    visual = !gitk
    stash-unapply = !git stash show -p | git apply -R
[apply]
    whitespace = fix
[user]
    name = Lee Skillen
    email = lskillen@vulcanft.com
[core]
    editor = vim
    whitespace = trailing-space, space-before-tab
[merge]
    tool = kdiff3
[format]
    headers = "Organization: Vulcan Financial Technologies\n"
    numbered = auto
    signoff = true
    coverletter = auto
    keepsubject = true
[push]
    default = simple
[sendemail]
    confirm = always
    smtpencryption = tls
    smtpserver = smtp.gmail.com
    smtpuser = lskillen@vulcanft.com
    smtpserverport = 587
    smtpsslcertpath = /etc/ssl/certs/ca-bundle.trust.crt
    suppresscc = self
    attachcopy = true
    attachcopyencoding = base64

